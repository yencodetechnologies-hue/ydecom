# Loaded by Vite based on mode:
# - npm run dev  -> .env.development  (local API via Vite proxy)
# - npm run build -> .env.production   (live API)
#
# Do not put VITE_API_URL in a plain .env file — it applies to all modes.
